{
  "name": "tg-max-bridge-cloudflare-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.js",
    "test": "node --test test/*.test.js",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
